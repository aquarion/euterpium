name: "[Auto] Artifacts Management"

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  cleanup:
    permissions:
      actions: write
    uses: istic/shared-workflows/.github/workflows/auto-manage-artifacts.yml@main
